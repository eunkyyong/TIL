import sys
sys.stdin =

# pos을 기준으로 pos-2, pos-1, pos+1, pos+2에 있는 데이터 중
def getValue(pos):
    #1) t=pos-2, pos-1, pos+1, pos+2 위치 값 중 제일 큰 값을 구한다.
#2) pos의 값과 비교하여 pos에 있는 값이 크면 return pos의 값 -t
    else:
    return 0

T=10
for tc in range(1, T+1):
    N= int(input())
    BLD = list(map(lnt, input().split()))
